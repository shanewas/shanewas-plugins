# Implementation Plans

Generated by the improve skill on 2026-09-16 against commit `3c53bae`
(shallow-clone SHA — executors: run the drift check in your plan; if the
real repo history differs, treat mismatch as STOP). Execute in the order
below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row
when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI fails on py/js syntax errors | P1 | S | — | DONE |
| 002 | README layout lists all plugins | P1 | S | — | DONE |
| 003 | Ship add-plugin.sh | P2 | S | — (see notes) | DONE |
| 004 | Scope zero-dependency claim | P2 | S | — | DONE |
| 005 | Antigravity snippets installable | P2 | S | — | DONE |
| 006 | Native Muse hook wiring | P1 | M | 001 | DONE |
| 007 | Verify harness matrix | P2 | M | 006 (Muse row only) | DONE |
| 008 | Slop length bias: measure, document/fix | P3 | M | 001 | DONE (branch 2b: length-scaled fix) |
| 009 | Concern proxy without double-count | P3 | M | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first: syntax gate watches all later code edits. 002/004/005 are docs-only and may run in parallel with 001.
- 003 overlaps 001's CI step (adds `bash -n`): if 001 has not landed, the 003 executor creates the step and reports it.
- 006 needs a live Muse install to copy the envelope from; without one it BLOCKS by design (no guessed schemas).
- 007's Muse row wants 006 done; the other four rows are independent.
- 008/009 both touch scorers; run sequentially (either order) to avoid confusing test failures, not in parallel.

## Findings considered and rejected

- Bus factor 1 / personal email in manifests: process issue, no code plan; revisit if the project gains contributors.
- Fail-open exits and never-throw shims: deliberate documented design, not bugs.
- Real linting (ruff/flake8/eslint): conflicts with the repo's zero-dependency ethos; needs a maintainer decision first.
- Antigravity installer script: rejected in favor of the sed one-liner (plan 005) — less to maintain.
