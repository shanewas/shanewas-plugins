{
  "_note": "Merge this entry into your project's .agents/hooks.json, replacing /path/to/verify-done with your checkout. One-liner (run from the plugin dir): sed \"s#/path/to/verify-done#$(pwd)#g\" hooks/antigravity-hooks.json > /tmp/verify-done-hooks.json, then merge /tmp/verify-done-hooks.json.",
  "verify-done": {
    "SessionStart": null,
    "PreInvocation": null,
    "PostInvocation": null,
    "Stop": null,
    "PreToolUse": null,
    "PostToolUse": [
      {
        "matcher": "write_to_file|replace_file_content",
        "hooks": [
          {
            "type": "command",
            "command": "python /path/to/verify-done/tools/donecheck.py check",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
