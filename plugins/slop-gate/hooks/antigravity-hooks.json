{
  "_note": "Merge this entry into your project's .agents/hooks.json, replacing /path/to/slop-gate with your checkout. One-liner (run from the plugin dir): sed \"s#/path/to/slop-gate#$(pwd)#g\" hooks/antigravity-hooks.json > /tmp/slop-gate-hooks.json, then merge /tmp/slop-gate-hooks.json.",
  "slop-gate": {
    "SessionStart": null,
    "PreInvocation": null,
    "PostInvocation": null,
    "Stop": null,
    "PreToolUse": null,
    "PostToolUse": [
      {
        "matcher": "write_to_file|replace_file_content",
        "hooks": [
          {
            "type": "command",
            "command": "python /path/to/slop-gate/tools/slop.py check --json",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
