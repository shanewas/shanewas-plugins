{
  "_note": "Merge this entry into your project's .agents/hooks.json, replacing /path/to/commit-gate with your checkout. One-liner (run from the plugin dir): sed \"s#/path/to/commit-gate#$(pwd)#g\" hooks/antigravity-hooks.json > /tmp/commit-gate-hooks.json, then merge /tmp/commit-gate-hooks.json.",
  "commit-gate": {
    "SessionStart": null,
    "PreInvocation": null,
    "PostInvocation": null,
    "Stop": null,
    "PreToolUse": null,
    "PostToolUse": [
      {
        "matcher": "write_to_file|replace_file_content",
        "hooks": [
          {
            "type": "command",
            "command": "python /path/to/commit-gate/tools/commitcheck.py check --json",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
